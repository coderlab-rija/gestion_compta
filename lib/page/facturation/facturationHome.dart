import 'package:flutter/material.dart';

class Facturationhome extends StatelessWidget {
  const Facturationhome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
